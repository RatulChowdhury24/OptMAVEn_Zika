# .bashrc

# User specific aliases and functions

function takeover { qsub -I -l nodes=1:ppn=2 -l walltime=06:00:00 -j oe -q lionxj-cdm; }
function takeover2 { qsub -I -l nodes=1:ppn=2 -l walltime=48:00:00 -j oe -q lionxj-cdm; }
# Source global definitions
if [ -f /etc/bashrc ]; then
        . /etc/bashrc
fi

ILOG_LICENSE_FILE=/usr/global/ilog/ilm/access.ilm
export ILOG_LICENSE_FILE

LD_LIBRARY_PATH=/usr/global/ilog/opl401/bin
export LD_LIBRARY_PATH

module load gams
module load python/2.7.5.ucs4

#alias matlab='/usr/global/bin/matlab -nosplash -nodesktop'
alias xfig='xfig -nosplash -specialtext -latexfonts -startlatexFont default'
alias today='find ./ -mtime 1 -print'
alias findn='find ./ -iname'
alias q='quota -v'
alias gv='ggv'
alias qs='qstat -u $USER'

group='/gpfs/group/cdm/IPRO_Suite/'

export CVSROOT='$HOME/cvsroot'
export CVS_RSH ssh

export SVN_EDITOR='/usr/bin/vim'
umask 077

#set -o vi
set -o nounset

#PATH=$HOME/bin:$HOME/usr/bin:$HOME/local/bin/:/usr/global/python-2.4.1/bin:$PATH
PATH=$HOME/bin:$HOME/usr/bin:$HOME/local/bin/:$PATH

# Automatically added by module
shopt -s expand_aliases
